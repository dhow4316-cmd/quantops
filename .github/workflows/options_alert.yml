name: QUANTOPS Options Alert Agent

on:
  schedule:
    # Runs every 5 minutes
    - cron: '*/5 * * * *'
  workflow_dispatch:  # Manual trigger

jobs:
  options-scan:
    runs-on: ubuntu-latest
    timeout-minutes: 4  # Kill before next 5-min cycle

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests

      - name: Run Options Alert Agent (single scan)
        env:
          TELEGRAM_TOKEN:   ${{ 8616658610:AAHUtpzBW4XTSUdefwsk_5fwSpKdPru2to}}
          TELEGRAM_CHAT_ID: ${{ 6598485796 }}
          BYBIT_API_KEY:    ${{ irg4WOqoPdOJoMos3u }}
          BYBIT_API_SECRET: ${{ Q87UAgAHmg78jO0Fa2kC0Mvt4QR7f2nSuRwU8616658610 }}
        run: python options_alert_single.py
